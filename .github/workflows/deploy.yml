name: 🚀 Deploy to VPS

on:
  push:
    branches:
      - main  # or master if your main branch is named differently

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy on VPS

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add VPS to known_hosts
        run: |
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy via SSH
        run: |
          ssh ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
          set -e
          echo "🚀 Starting deployment on VPS..."

          # Go to or create the deployment folder
          mkdir -p /var/www/xfinai
          cd /var/www/xfinai

          # Clone repo if not exists
          if [ ! -d ".git" ]; then
            git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git .
          fi

          git pull origin main

          echo "📦 Installing backend dependencies..."
          cd backend
          python3 -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt
          deactivate
          cd ..

          echo "🧠 Restarting backend + cron job..."
          # Kill any old running instance of admin_panel.py
          pkill -f admin_panel.py || true
          cd backend
          nohup python3 admin_panel.py > /var/www/xfinai/backend.log 2>&1 &

          echo "⚙️ Building frontend..."
          cd ../frontend
          npm install
          npm run build

          echo "⚙️ Building admin frontend..."
          cd ../frontend-admin
          npm install
          npm run build

          echo "✅ Deployment complete!"
          EOF
